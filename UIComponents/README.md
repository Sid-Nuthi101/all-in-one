# UIComponents

Reusable, plug-and-play UI components built with PySide6.
These components are UI-only and reference shared design tokens for a cohesive
futuristic glassmorphism look.

Import examples:

```python
from UIComponents.primitives.glass_panel import GlassPanel
from UIComponents.backgrounds.background_stack import BackgroundStack
```
